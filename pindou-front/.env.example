# ============ 前端环境变量（复制为 .env 后按需修改） ============
# 说明：所有以 VITE_ 开头的变量会在构建时被注入到客户端代码，切勿存放密钥。

# API 基地址（后端与静态资源 /uploads 的统一入口）
#   - 开发环境：Vite 已代理 /api 到 http://localhost:3000，但 /uploads 图片需直连后端，
#     因此默认指向本机后端端口。
#   - 生产环境：docker-compose 构建镜像时会注入 PUBLIC_BASE_URL（前端 Nginx 入口），
#     由 Nginx 统一转发 /api 与 /uploads 到后端。
VITE_API_BASE=http://localhost:3000
